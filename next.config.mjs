/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'placeimg.com', 'encrypted-tbn0.gstatic.com', 'phantom-elmundo.unidadeditorial.es', 'api.escuelajs.co', 'i.imgur.com', 'www.pexels.com', 'eduport.webestica.com', 'cdn.baguer.co']
    }
};

export default nextConfig;
